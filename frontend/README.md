# frontend

React + TypeScript SPA (Vite, Mantine UI, axios, React Router), consuming the
`backend` REST API. Scaffolding lands in Phase 1 (Auth & RBAC), alongside the
login screen — see the repo root README's roadmap table.
